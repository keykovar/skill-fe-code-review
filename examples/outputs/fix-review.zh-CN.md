# Fix Review 中文输出示例

```md
## 回审结论

回审建议：修改后再次回审

- 已解决：1
- 部分解决：1
- 未解决：0
- 无法验证：0
- 新增回归：1 个 Risk

## 回审范围

- 请求范围：复核上一轮 2 个 findings 的修复状态，并检查修复直接引入的回归。
- 上次审查基线：上一轮 Review 的 2 个 findings
- 原始变更范围：`main...feature/profile-cache`
- 本次修复范围：`abc1234..HEAD`
- 比较基线：修复前提交 `abc1234` 与当前 `HEAD`
- 涉及文件与调用链：profile request -> cache owner -> page render
- 已修改：`src/features/profile/useProfile.ts` 及其测试。
- 已暂存：无；本次为提交范围回审。
- 未暂存：无。
- 未跟踪：无。
- 已执行验证：单测、type-check
- 跳过的验证：浏览器断网后的实际恢复路径；缺少可控且可重复重建的断网 fixture。

## Issue Verification：问题验证

- [src/features/profile/useProfile.ts:42] 请求失败后仍保留旧资料
  - 原严重级别：Risk
  - 当前状态：Resolved：已解决
  - 修复前行为：失败分支只更新 loading，继续暴露旧资料。
  - 修复后行为：失败分支清空当前请求拥有的数据。
  - 当前证据：失败分支和对应单测均覆盖清理行为。
  - 剩余风险：无明确风险。
  - 验证结果：失败请求单测通过；未做浏览器断网手测。

- [src/features/profile/useProfile.ts:58] 重试期间必须保留最后一次成功结果
  - 原严重级别：Risk
  - 当前状态：Partially Resolved：部分解决
  - 修复前行为：重试失败后旧资料一直保留。
  - 修复后行为：所有失败都会清空资料，包括后台重试失败。
  - 当前证据：修复解决了首次加载失败，但改变了后台重试语义。
  - 剩余风险：短暂网络错误会清空仍可展示的最后一次成功结果。
  - 验证结果：缺少后台重试失败用例。

## New Regression：新增回归

### Blocking：必须修改

无明确问题。

### Risk：建议修改

- [src/features/profile/useProfile.ts:61] 后台重试失败会清空有效缓存
  - 触发场景：页面已有成功结果，后台刷新请求失败。
  - 影响：页面从有效资料退化为空状态。
  - 根因：首次加载和后台刷新共用同一个失败清理分支。
  - 建议方案：按请求阶段区分是否清理当前数据。
  - 验证方式：增加“已有数据 + 后台刷新失败”用例。

### Improve：可优化

无明确问题。

## Behavior Delta：行为差异

- 预期修复：首次加载失败时不再暴露旧请求数据。
- 实际变化：所有请求失败都会清空数据。
- 应保持不变的行为：后台刷新失败时继续展示最后一次成功结果。
- 意外变化或缺失：后台刷新保留缓存的行为丢失。

## Test Gaps：测试缺口

- 原问题复现：已覆盖首次加载失败。
- 修复验证：对应单测通过。
- 回归测试：缺少后台刷新失败。
- 运行时 / 监控验证：未做浏览器断网手测。

## Evidence：证据

- 上次问题清单：上一轮 Review 的 2 个 Risk findings。
- fix diff：`abc1234..HEAD`。
- 本地代码与调用链：失败分支、缓存 owner 和页面 consumer。
- package/version：未涉及依赖变化。
- 官方文档核验：不需要；问题由本地状态转换和调用链证据支持。
- 浏览器运行证据：未执行；原 findings 可由本地状态转换和聚焦单测判断，浏览器断网不属于其关闭条件；当前也没有满足安全 gate 的可控断网 fixture。
- 未验证：真实断网与恢复时序；这是补充运行时路径，不属于原 findings 的验收条件，因此不改变上方回审状态。

## 最终建议

第一个问题可以关闭；第二个问题仍需区分首次加载和后台刷新失败，修复后再次执行 Fix Review。
```
